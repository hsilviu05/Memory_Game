<UserControl x:Class="Memory_Game.View.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Memory_Game.View"
             xmlns:vm="clr-namespace:Memory_Game.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="#1E1E1E">

    <UserControl.DataContext>
        <vm:StatisticsViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="HeaderText"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="24"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
            <Setter Property="Foreground"
                    Value="#FFFFFF"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
            <Setter Property="Margin"
                    Value="0,0,0,20"/>
        </Style>

        <Style x:Key="StatLabel"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="16"/>
            <Setter Property="Foreground"
                    Value="#CCCCCC"/>
            <Setter Property="Margin"
                    Value="0,5"/>
        </Style>

        <Style x:Key="StatValue"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="16"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
            <Setter Property="Foreground"
                    Value="#FFFFFF"/>
            <Setter Property="Margin"
                    Value="10,5"/>
        </Style>

        <Style x:Key="DataGridStyle"
               TargetType="DataGrid">
            <Setter Property="Background"
                    Value="#2D2D2D"/>
            <Setter Property="Foreground"
                    Value="#FFFFFF"/>
            <Setter Property="GridLinesVisibility"
                    Value="None"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="RowBackground"
                    Value="#2D2D2D"/>
            <Setter Property="AlternatingRowBackground"
                    Value="#333333"/>
        </Style>
    </UserControl.Resources>

    <Border Margin="20"
            Background="#2D2D2D"
            CornerRadius="10"
            BorderBrush="#007ACC"
            BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Game Statistics"
                       Style="{StaticResource HeaderText}"/>

            <!-- Overall Statistics -->
            <Grid Grid.Row="1"
                  Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Total Games -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Games"
                               Style="{StaticResource StatLabel}"/>
                    <TextBlock Text="{Binding TotalGamesPlayed}"
                               Style="{StaticResource StatValue}"/>
                </StackPanel>

                <!-- Average Win Rate -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Average Win Rate"
                               Style="{StaticResource StatLabel}"/>
                    <TextBlock Style="{StaticResource StatValue}">
                        <Run Text="{Binding AverageWinRate, StringFormat='{}{0:F1}'}"/>
                        <Run Text="%"/>
                    </TextBlock>
                </StackPanel>

                <!-- Best Time -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Best Overall Time"
                               Style="{StaticResource StatLabel}"/>
                    <TextBlock Text="{Binding BestOverallTime, StringFormat='{}{0:mm}:{0:ss}'}"
                               Style="{StaticResource StatValue}"/>
                </StackPanel>

                <!-- Games Won -->
                <StackPanel Grid.Row="1"
                            Grid.Column="0">
                    <TextBlock Text="Total Wins"
                               Style="{StaticResource StatLabel}"/>
                    <TextBlock Text="{Binding TotalGamesWon}"
                               Style="{StaticResource StatValue}"/>
                </StackPanel>

                <!-- Average Moves -->
                <StackPanel Grid.Row="1"
                            Grid.Column="1">
                    <TextBlock Text="Avg. Moves per Game"
                               Style="{StaticResource StatLabel}"/>
                    <TextBlock Text="{Binding AverageMovesPerGame, StringFormat='{}{0:F1}'}"
                               Style="{StaticResource StatValue}"/>
                </StackPanel>
            </Grid>

            <!-- Player Statistics Table -->
            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding AllStatistics}"
                      SelectedItem="{Binding SelectedUserStats}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Style="{StaticResource DataGridStyle}"
                      Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Player"
                                        Binding="{Binding Username}"
                                        Width="*"/>
                    <DataGridTextColumn Header="Games"
                                        Binding="{Binding GamesPlayed}"
                                        Width="Auto"/>
                    <DataGridTextColumn Header="Wins"
                                        Binding="{Binding GamesWon}"
                                        Width="Auto"/>
                    <DataGridTextColumn Header="Win Rate"
                                        Binding="{Binding WinRate, StringFormat={}{0:F1}%}"
                                        Width="Auto"/>
                    <DataGridTextColumn Header="Best Time"
                                        Binding="{Binding BestTime, StringFormat={}{0:mm}:{0:ss}}"
                                        Width="Auto"/>
                    <DataGridTextColumn Header="Avg. Moves"
                                        Binding="{Binding AverageMovesPerGame, StringFormat={}{0:F1}}"
                                        Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Close Button -->
            <Button Grid.Row="3"
                    Content="Close"
                    Command="{Binding CloseCommand}"
                    Width="100"
                    Height="30"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</UserControl>
